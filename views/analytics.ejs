<%- include("partials/header", { title: `OrganizaPay | Análises ${String(month).padStart(2,'0')}/${year}` }) %>
<%
  const totalKpis = summaryCharts && summaryCharts.kpis ? summaryCharts.kpis : {};
  const analyticsSource = analyticsCharts || {};
  const graphKpis = analyticsSource && analyticsSource.kpis ? analyticsSource.kpis : {};
  const graphInsights = analyticsSource && analyticsSource.insights ? analyticsSource.insights : {};
  const merchantRanking = analyticsSource && Array.isArray(analyticsSource.establishments) ? analyticsSource.establishments : [];
  const merchantTop = graphInsights && graphInsights.topMerchant ? graphInsights.topMerchant : null;
  const merchantSuggestions = analyticsSource && Array.isArray(analyticsSource.establishmentSuggestions) ? analyticsSource.establishmentSuggestions : [];
  const merchantLearning = graphInsights && graphInsights.merchantLearning ? graphInsights.merchantLearning : { activeRules: [], pausedRules: [] };
  const cardTrendByCard = analyticsSource && Array.isArray(analyticsSource.cardTrendByCard) ? analyticsSource.cardTrendByCard : [];
  const merchantSectionDefaultOpen = true;
  const merchantRankingDefaultOpen = true;
  const financeTotals = financeAnalytics && financeAnalytics.totals ? financeAnalytics.totals : { incomeCents: 0, expenseCents: 0, netCents: 0, incomeRowCount: 0, expenseRowCount: 0, positiveNetMonths: 0 };
  const financeTrend = analyticsSource && Array.isArray(analyticsSource.flowTrend) ? analyticsSource.flowTrend : [];
  const financeIncomeSources = financeAnalytics && Array.isArray(financeAnalytics.incomeSources) ? financeAnalytics.incomeSources : [];
  const financeExpenseSources = financeAnalytics && Array.isArray(financeAnalytics.expenseSources) ? financeAnalytics.expenseSources : [];
  const financeTrendMax = Math.max(1, ...financeTrend.map((point) => Math.max(Number(point.income_cents || 0), Number(point.expense_cents || 0), Number(point.card_cents || 0))));
  const cardSpendCents = Number(graphKpis.totalSpentCents || 0);
  const outsideExpenseCents = Number(financeTotals.expenseCents || 0);
  const totalOutflowCents = outsideExpenseCents + cardSpendCents;
  const overallNetCents = Number(financeTotals.incomeCents || 0) - totalOutflowCents;
  const totalCommittedCents = totalOutflowCents;
  const peopleRows = Array.isArray(peopleBreakdown) ? peopleBreakdown : [];
  const cardRows = Array.isArray(cardBreakdown) ? cardBreakdown : [];
  const hasFinanceFlow = financeTrend.some((point) => Number(point.income_cents || 0) > 0 || Number(point.expense_cents || 0) > 0 || Number(point.card_cents || 0) > 0);
%>

<div class="op-screen op-screen--summary op-screen--analytics w-full">
  <section class="op-page-hero op-fade-up mb-8">
    <div>
      <span class="op-page-hero__eyebrow">Raio-x do mês</span>
      <h1 class="op-page-hero__title mt-4">Análises do mês — <%= String(month).padStart(2,'0') %>/<%= year %></h1>
      <p class="op-page-hero__subtitle mt-3">Aqui você bate o olho no que pesou, no que entrou, no que saiu e no que merece um carinho extra.</p>
      <div class="op-page-hero__meta mt-4">
        <span class="badge">Ficou nos cartões: <%= formatBRLFromCents(cardSpendCents) %></span>
        <span class="badge">Grana que entrou: <%= formatBRLFromCents(financeTotals.incomeCents || 0) %></span>
        <span class="badge">Saiu no mês: <%= formatBRLFromCents(outsideExpenseCents) %></span>
        <span class="badge"><%= merchantTop ? `Lugar que mais pesou: ${merchantTop.label}` : 'Quando o mês ganhar movimento, o lugar campeão aparece aqui' %></span>
      </div>
    </div>
    <div class="op-page-hero__actions op-page-hero__actions--compact">
      <a href="/month/<%= year %>/<%= month %>" class="op-btn op-btn-success op-btn--compact whitespace-nowrap">
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 10h16M4 14h16M4 18h16"></path>
        </svg>
        Ver compras
      </a>
      <a href="/summary/<%= year %>/<%= month %>" class="op-btn op-btn-info op-btn--compact whitespace-nowrap">
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v8m4-4H8"></path>
        </svg>
        Fechamento
      </a>
      <a href="/detalhamento/<%= year %>/<%= month %>" class="op-btn op-btn-export op-btn--compact whitespace-nowrap">
        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 17v-6m4 6V7m4 10v-3M5 19h14"></path>
        </svg>
        Entradas e saídas
      </a>
    </div>
  </section>

  <section class="mb-8 rounded-[28px] border border-slate-200 bg-white/95 p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800/90 lg:p-6" data-op-tabs>
    <div class="flex flex-col gap-4 xl:flex-row xl:items-end xl:justify-between">
      <div>
        <div class="inline-flex items-center gap-2 rounded-full border border-violet-200 bg-violet-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.24em] text-violet-700 dark:border-violet-800 dark:bg-violet-900/20 dark:text-violet-200">Leitura esperta</div>
        <h2 class="mt-3 text-2xl font-black tracking-tight text-slate-900 dark:text-white">Seu mês, sem enrolação e sem conta misturada</h2>
        <p class="mt-2 max-w-3xl text-sm leading-6 text-slate-600 dark:text-slate-300">Passe entre o resumão, o entra e sai, a divisão dos cartões e os lugares que mais pesaram no seu bolso.</p>
      </div>
      <div class="flex flex-wrap items-center gap-2 xl:justify-end">
        <a href="/analytics/<%= previousSummaryRef.year %>/<%= previousSummaryRef.month %>" class="inline-flex items-center justify-center rounded-2xl border border-slate-200 px-4 py-2 text-sm font-bold text-slate-700 transition hover:-translate-y-0.5 hover:border-slate-300 hover:bg-slate-50 dark:border-slate-600 dark:text-slate-200 dark:hover:bg-slate-700/60">← <span class="ml-2">Mês anterior</span></a>
        <a href="/analytics/<%= nextSummaryRef.year %>/<%= nextSummaryRef.month %>" class="inline-flex items-center justify-center rounded-2xl border border-slate-200 px-4 py-2 text-sm font-bold text-slate-700 transition hover:-translate-y-0.5 hover:border-slate-300 hover:bg-slate-50 dark:border-slate-600 dark:text-slate-200 dark:hover:bg-slate-700/60"><span class="mr-2">Próximo mês</span>→</a>
      </div>
    </div>

    <div class="mt-5 op-tab-strip" role="tablist" aria-label="Blocos das análises">
      <button type="button" class="op-tab-btn is-active" role="tab" aria-selected="true" data-op-tab-target="overview"><span class="sm:hidden">Resumão</span><span class="hidden sm:inline">Visão geral</span></button>
      <button type="button" class="op-tab-btn" role="tab" aria-selected="false" data-op-tab-target="flow"><span class="sm:hidden">Entra e sai</span><span class="hidden sm:inline">Entradas e saídas</span></button>
      <button type="button" class="op-tab-btn" role="tab" aria-selected="false" data-op-tab-target="split"><span class="sm:hidden">Turma e cartões</span><span class="hidden sm:inline">Pessoas e cartões</span></button>
      <button type="button" class="op-tab-btn" role="tab" aria-selected="false" data-op-tab-target="places">Lugares</button>
    </div>

    <div class="mt-6 space-y-6">
      <div data-op-tab-panel="overview">
        <div class="grid gap-3 lg:grid-cols-4">
          <div class="rounded-[24px] border border-emerald-200 bg-emerald-50/90 p-4 dark:border-emerald-900/60 dark:bg-emerald-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-emerald-700 dark:text-emerald-300">Grana que entrou</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-emerald-800 dark:text-emerald-100"><%= formatBRLFromCents(financeTotals.incomeCents || 0) %></div>
            <p class="mt-2 text-xs leading-5 text-emerald-800/80 dark:text-emerald-100/80"><%= financeIncomeSources[0] ? `${financeIncomeSources[0].label} foi quem mais deu aquela força.` : 'Quando entrar grana por aqui, ela aparece sem drama.' %></p>
          </div>
          <div class="rounded-[24px] border border-rose-200 bg-rose-50/90 p-4 dark:border-rose-900/60 dark:bg-rose-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-rose-700 dark:text-rose-300">Saiu no mês</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-rose-800 dark:text-rose-100"><%= formatBRLFromCents(financeTotals.expenseCents || 0) %></div>
            <p class="mt-2 text-xs leading-5 text-rose-800/80 dark:text-rose-100/80"><%= financeExpenseSources[0] ? `${financeExpenseSources[0].label} puxou mais essa parte.` : 'Se pintar saída por fora, ela aparece aqui.' %></p>
          </div>
          <div class="rounded-[24px] border border-amber-200 bg-amber-50/90 p-4 dark:border-amber-900/60 dark:bg-amber-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-amber-700 dark:text-amber-300">Ficou nos cartões</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-amber-900 dark:text-amber-100"><%= formatBRLFromCents(cardSpendCents) %></div>
            <p class="mt-2 text-xs leading-5 text-amber-800/80 dark:text-amber-100/80"><%= graphKpis.topCategory ? `${graphKpis.topCategory.label} levou ${graphKpis.topCategory.share_pct || 0}% do seu pedaço no cartão.` : 'Quando você dividir as compras, esse cantinho mostra seu pedaço certinho.' %></p>
          </div>
          <div class="rounded-[24px] border border-slate-200 bg-slate-900 p-4 text-white dark:border-slate-700 dark:bg-slate-950">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-300">Quanto sobra</div>
            <div class="mt-2 text-2xl font-black tracking-tight"><%= formatBRLFromCents(overallNetCents) %></div>
            <p class="mt-2 text-xs leading-5 text-slate-300"><%= overallNetCents >= 0 ? 'Depois de juntar tudo, ainda sobrou um respiro.' : 'Aqui já dá para ver que o mês apertou mais do que devia.' %></p>
          </div>
        </div>

        <section class="mt-6 op-surface-card rounded-[28px] border border-slate-200 bg-white/95 p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800/90 lg:p-6" data-summary-graphs-root data-summary-year="<%= year %>" data-summary-month="<%= month %>" data-summary-graphs-ready="false" data-summary-detail-base="analytics">
          <div class="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between">
            <div>
              <div class="inline-flex items-center gap-2 rounded-full border border-violet-200 bg-violet-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.24em] text-violet-700 dark:border-violet-800 dark:bg-violet-900/20 dark:text-violet-200">Seu retrato no cartão</div>
              <h3 class="mt-3 text-2xl font-black tracking-tight text-slate-900 dark:text-white">O que puxou seu mês e como isso vem andando</h3>
              <p class="mt-2 max-w-3xl text-sm leading-6 text-slate-600 dark:text-slate-300">Aqui entra o seu pedaço no cartão: por categoria e mês a mês, sem misturar com a conta inteira da turma.</p>
            </div>
            <div class="grid gap-3 sm:grid-cols-2 xl:w-[25rem]">
              <div class="rounded-[22px] border border-slate-200 bg-slate-50/80 p-4 dark:border-slate-700 dark:bg-slate-900/50">
                <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-500 dark:text-slate-400">Radar do mês</div>
                <div class="mt-2 text-lg font-black tracking-tight text-slate-900 dark:text-white">
                  <% if (graphInsights.currentVsAveragePct === null || graphInsights.currentVsAveragePct === undefined) { %>
                    Ainda sem chão para comparar
                  <% } else if (Number(graphInsights.currentVsAveragePct || 0) > 0) { %>
                    <%= Number(graphInsights.currentVsAveragePct || 0) %>% acima da média recente
                  <% } else if (Number(graphInsights.currentVsAveragePct || 0) < 0) { %>
                    <%= Math.abs(Number(graphInsights.currentVsAveragePct || 0)) %>% abaixo da média dos últimos meses
                  <% } else { %>
                    No mesmo pique da média recente
                  <% } %>
                </div>
                <p class="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400">Média recente: <span class="font-bold text-slate-800 dark:text-slate-100"><%= formatBRLFromCents(graphInsights.averageMonthlySpendCents || 0) %></span>.</p>
              </div>
              <div class="rounded-[22px] border border-slate-200 bg-slate-50/80 p-4 dark:border-slate-700 dark:bg-slate-900/50">
                <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-500 dark:text-slate-400">Categorias em dia</div>
                <div class="mt-2 text-lg font-black tracking-tight text-slate-900 dark:text-white"><%= Number(graphKpis.categoryCoveragePct || 0) %>% em ordem</div>
                <p class="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400"><%= formatBRLFromCents(graphKpis.uncategorizedCents || 0) %> ainda está sem categoria certinha.</p>
              </div>
            </div>
          </div>

          <div class="mt-5 flex flex-wrap items-center gap-2 rounded-2xl border border-slate-200 bg-slate-50/80 px-3 py-3 text-sm text-slate-600 dark:border-slate-700 dark:bg-slate-900/40 dark:text-slate-300">
            <span class="font-bold text-slate-800 dark:text-slate-100">Dica:</span>
            <span>Tocou numa categoria ou num mês, a gente te leva direto para as compras daquele pedaço.</span>
            <div class="hidden sm:block h-4 w-px bg-slate-300 dark:bg-slate-700"></div>
            <span data-summary-interaction-hint class="font-medium text-slate-500 dark:text-slate-400">Toque para fuçar sem medo.</span>
          </div>

          <div class="mt-6 grid gap-4 xl:grid-cols-2">
            <div class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
              <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
                <div class="min-w-0">
                  <h3 class="text-lg font-black text-slate-900 dark:text-white">Seu pedaço por categoria</h3>
                  <p class="mt-1 text-sm leading-6 text-slate-500 dark:text-slate-400">Mostra para onde foi o seu pedaço do cartão neste mês.</p>
                </div>
                <span class="inline-flex items-center self-start rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Categorias</span>
              </div>
              <div class="mt-4 flex flex-col gap-4 2xl:flex-row 2xl:items-center">
                <div class="w-full 2xl:max-w-[320px]" data-summary-donut-chart><div class="flex min-h-[260px] animate-pulse items-center justify-center rounded-[24px] border border-slate-200 bg-slate-50/80 px-6 py-10 text-center text-sm font-semibold leading-6 text-slate-400 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-500">Assando a pizza financeira...</div></div>
                <div class="min-w-0 flex-1" data-summary-donut-legend><div class="grid gap-3 sm:grid-cols-2 2xl:grid-cols-1"><div class="h-20 animate-pulse rounded-2xl border border-slate-200 bg-slate-50/80 dark:border-slate-700 dark:bg-slate-800/60"></div><div class="h-20 animate-pulse rounded-2xl border border-slate-200 bg-slate-50/80 dark:border-slate-700 dark:bg-slate-800/60"></div><div class="h-20 animate-pulse rounded-2xl border border-slate-200 bg-slate-50/80 dark:border-slate-700 dark:bg-slate-800/60"></div><div class="h-20 animate-pulse rounded-2xl border border-slate-200 bg-slate-50/80 dark:border-slate-700 dark:bg-slate-800/60"></div></div></div>
              </div>
            </div>

            <div class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
              <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
                <div class="min-w-0">
                  <h3 class="text-lg font-black text-slate-900 dark:text-white">Seu cartão, mês a mês</h3>
                  <p class="mt-1 text-sm leading-6 text-slate-500 dark:text-slate-400">Bom para ver quando o seu pedaço no cartão engrossou ou deu uma respirada.</p>
                </div>
                <span class="inline-flex items-center self-start rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Últimos 6 meses</span>
              </div>
              <div class="mt-4" data-summary-line-chart><div class="flex min-h-[260px] animate-pulse items-center justify-center rounded-[24px] border border-slate-200 bg-slate-50/80 px-6 py-10 text-center text-sm font-semibold leading-6 text-slate-400 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-500">Afinando a linha do tempo das compras...</div></div>
              <div class="mt-4 grid gap-3 sm:grid-cols-2">
                <div class="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 dark:border-slate-700 dark:bg-slate-800/70">
                  <div class="text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:text-slate-400">Comparação com o mês anterior</div>
                  <div class="mt-2 text-lg font-black <%= Number(graphKpis.deltaCents || 0) > 0 ? 'text-rose-600 dark:text-rose-300' : 'text-emerald-600 dark:text-emerald-300' %>"><%= Number(graphKpis.deltaCents || 0) > 0 ? '+' : '' %><%= formatBRLFromCents(graphKpis.deltaCents || 0) %></div>
                  <p class="mt-1 text-xs leading-5 text-slate-500 dark:text-slate-400"><% if (graphKpis.deltaPct === null || graphKpis.deltaPct === undefined) { %>Ainda sem chão para comparar.<% } else { %><%= Math.abs(Number(graphKpis.deltaPct || 0)) %>% <%= Number(graphKpis.deltaPct || 0) >= 0 ? 'acima' : 'abaixo' %> do mês anterior.<% } %></p>
                </div>
                <div class="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 dark:border-slate-700 dark:bg-slate-800/70">
                  <div class="text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:text-slate-400">Pico recente</div>
                  <div class="mt-2 text-lg font-black text-slate-900 dark:text-white"><%= graphInsights.peakPoint ? graphInsights.peakPoint.label : 'Ainda sem pico' %></div>
                  <p class="mt-1 text-xs leading-5 text-slate-500 dark:text-slate-400"><%= graphInsights.peakPoint ? `${formatBRLFromCents(graphInsights.peakPoint.total_cents || 0)} foi o maior total dos últimos meses.` : 'Assim que tiver histórico suficiente, o pico aparece aqui.' %></p>
                </div>
              </div>
            </div>
          </div>

          <div class="sr-only" aria-live="polite" data-summary-graphs-status>Os gráficos do mês já estão no jeito.</div>
          <script type="application/json" data-summary-graphs-json><%- JSON.stringify(analyticsCharts || { categories: [], monthlyTrend: [], kpis: {}, insights: {}, flowTrend: [] }) %></script>
        </section>
      </div>

      <div data-op-tab-panel="flow" hidden>
        <div class="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">
          <div class="rounded-[24px] border border-emerald-200 bg-emerald-50/90 p-4 dark:border-emerald-900/60 dark:bg-emerald-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-emerald-700 dark:text-emerald-300">Grana que entrou</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-emerald-800 dark:text-emerald-100"><%= formatBRLFromCents(financeTotals.incomeCents || 0) %></div>
            <p class="mt-2 text-xs leading-5 text-emerald-800/80 dark:text-emerald-100/80"><%= financeIncomeSources[0] ? `${financeIncomeSources[0].label} foi a maior força por aqui.` : 'Quando entrar grana, ela vai cair certinho aqui.' %></p>
          </div>
          <div class="rounded-[24px] border border-rose-200 bg-rose-50/90 p-4 dark:border-rose-900/60 dark:bg-rose-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-rose-700 dark:text-rose-300">Saiu no mês</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-rose-800 dark:text-rose-100"><%= formatBRLFromCents(outsideExpenseCents) %></div>
            <p class="mt-2 text-xs leading-5 text-rose-800/80 dark:text-rose-100/80"><%= financeExpenseSources[0] ? `${financeExpenseSources[0].label} puxou mais essa parte.` : 'Ainda não teve saída por fora registrada por aqui.' %></p>
          </div>
          <div class="rounded-[24px] border border-amber-200 bg-amber-50/90 p-4 dark:border-amber-900/60 dark:bg-amber-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-amber-700 dark:text-amber-300">Ficou nos cartões</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-amber-900 dark:text-amber-100"><%= formatBRLFromCents(cardSpendCents) %></div>
            <p class="mt-2 text-xs leading-5 text-amber-800/80 dark:text-amber-100/80"><%= cardSpendCents > 0 ? 'Aqui entra só o seu pedaço no cartão, sem misturar com o resto da turma.' : 'Quando você tiver valor no cartão, ele aparece aqui.' %></p>
          </div>
          <div class="rounded-[24px] border border-violet-200 bg-violet-50/90 p-4 dark:border-violet-900/60 dark:bg-violet-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-violet-700 dark:text-violet-300">Tudo comprometido</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-violet-900 dark:text-violet-100"><%= formatBRLFromCents(totalCommittedCents) %></div>
            <p class="mt-2 text-xs leading-5 text-violet-800/80 dark:text-violet-100/80">Saiu no mês + seu pedaço no cartão. Sem maquiagem.</p>
          </div>
          <div class="rounded-[24px] border border-slate-200 bg-slate-900 p-4 text-white dark:border-slate-700 dark:bg-slate-950">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-300">Quanto sobra</div>
            <div class="mt-2 text-2xl font-black tracking-tight"><%= formatBRLFromCents(overallNetCents) %></div>
            <p class="mt-2 text-xs leading-5 text-slate-300"><%= overallNetCents >= 0 ? 'Depois de tudo isso, ainda sobrou um respiro.' : 'Depois de tudo isso, o mês já começou a apertar.' %></p>
          </div>
        </div>

        <div class="mt-4 flex flex-col gap-3 rounded-[24px] border border-slate-200 bg-white/90 p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/50 lg:flex-row lg:items-center lg:justify-between">
          <div>
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-500 dark:text-slate-400">Quer ajeitar alguma peça?</div>
            <p class="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">Salário, contas da casa, extras e outras saídas por fora continuam no mesmo cantinho de sempre.</p>
          </div>
          <a href="/detalhamento/<%= year %>/<%= month %>" class="op-btn op-btn-export op-btn--compact whitespace-nowrap">Abrir entradas e saídas</a>
        </div>

        <% if (hasFinanceFlow) { %>
          <div class="mt-6 grid gap-4 xl:grid-cols-[1.35fr,1fr]">
            <section class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
              <div class="flex items-start justify-between gap-3">
                <div>
                  <h3 class="text-lg font-black text-slate-900 dark:text-white">Como essa dança foi andando</h3>
                  <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Aqui entram entradas, saídas por fora e o seu pedaço no cartão, tudo mês a mês.</p>
                </div>
                <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Últimos 6 meses</span>
              </div>
              <div class="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-3">
                <% financeTrend.forEach((point) => { 
                     const incomePct = financeTrendMax > 0 ? Math.max(8, Math.round((Number(point.income_cents || 0) / financeTrendMax) * 100)) : 0;
                     const expensePct = financeTrendMax > 0 ? Math.max(8, Math.round((Number(point.expense_cents || 0) / financeTrendMax) * 100)) : 0;
                     const cardPct = financeTrendMax > 0 ? Math.max(8, Math.round((Number(point.card_cents || 0) / financeTrendMax) * 100)) : 0;
                %>
                  <div class="rounded-[22px] border <%= point.is_current ? 'border-violet-300 bg-violet-50/70 dark:border-violet-800 dark:bg-violet-900/20' : 'border-slate-200 bg-slate-50/80 dark:border-slate-700 dark:bg-slate-800/60' %> p-4">
                    <div class="flex items-center justify-between gap-2">
                      <div class="text-[11px] font-bold uppercase tracking-[0.2em] <%= point.is_current ? 'text-violet-700 dark:text-violet-300' : 'text-slate-500 dark:text-slate-400' %>"><%= point.label %></div>
                      <span class="inline-flex items-center rounded-full px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.16em] <%= Number(point.net_cents || 0) >= 0 ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-900/30 dark:text-emerald-200' : 'bg-rose-100 text-rose-700 dark:bg-rose-900/30 dark:text-rose-200' %>"><%= Number(point.net_cents || 0) >= 0 ? 'sobrou' : 'apertou' %></span>
                    </div>
                    <div class="mt-3 space-y-3">
                      <div>
                        <div class="flex items-center justify-between gap-2 text-[11px] font-bold uppercase tracking-[0.18em] text-emerald-700 dark:text-emerald-300"><span>Entrou</span><span><%= formatBRLFromCents(point.income_cents || 0) %></span></div>
                        <div class="mt-1 h-2 rounded-full bg-emerald-100 dark:bg-emerald-950/40"><div class="h-2 rounded-full bg-emerald-500 dark:bg-emerald-400" style="width:<%= incomePct %>%"></div></div>
                      </div>
                      <div>
                        <div class="flex items-center justify-between gap-2 text-[11px] font-bold uppercase tracking-[0.18em] text-rose-700 dark:text-rose-300"><span>Saiu</span><span><%= formatBRLFromCents(point.expense_cents || 0) %></span></div>
                        <div class="mt-1 h-2 rounded-full bg-rose-100 dark:bg-rose-950/40"><div class="h-2 rounded-full bg-rose-500 dark:bg-rose-400" style="width:<%= expensePct %>%"></div></div>
                      </div>
                      <div>
                        <div class="flex items-center justify-between gap-2 text-[11px] font-bold uppercase tracking-[0.18em] text-amber-700 dark:text-amber-300"><span>Cartão</span><span><%= formatBRLFromCents(point.card_cents || 0) %></span></div>
                        <div class="mt-1 h-2 rounded-full bg-amber-100 dark:bg-amber-950/40"><div class="h-2 rounded-full bg-amber-500 dark:bg-amber-400" style="width:<%= cardPct %>%"></div></div>
                      </div>
                    </div>
                    <div class="mt-3 text-xs font-semibold <%= Number(point.net_cents || 0) >= 0 ? 'text-emerald-700 dark:text-emerald-300' : 'text-rose-700 dark:text-rose-300' %>">Quanto sobrou: <%= formatBRLFromCents(point.net_cents || 0) %></div>
                  </div>
                <% }) %>
              </div>
            </section>

            <section class="space-y-4">
              <div class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
                <div class="flex items-start justify-between gap-3">
                  <div>
                    <h3 class="text-lg font-black text-slate-900 dark:text-white">Quem mais trouxe grana</h3>
                    <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">As entradas ficam lado a lado para você bater o olho sem ficar caçando.</p>
                  </div>
                  <span class="inline-flex items-center rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-emerald-700 dark:border-emerald-900/60 dark:bg-emerald-950/20 dark:text-emerald-300"><%= financeIncomeSources.length %> fonte<%= financeIncomeSources.length === 1 ? '' : 's' %></span>
                </div>
                <% if (financeIncomeSources.length > 0) { %>
                  <div class="mt-4 space-y-3">
                    <% financeIncomeSources.slice(0, 6).forEach((item) => { %>
                      <div class="rounded-2xl border border-slate-200 bg-slate-50/80 px-4 py-3 dark:border-slate-700 dark:bg-slate-800/60">
                        <div class="flex items-center justify-between gap-3">
                          <div class="min-w-0">
                            <div class="truncate text-sm font-black text-slate-900 dark:text-white"><%= item.label %></div>
                            <div class="mt-1 text-xs font-semibold text-slate-500 dark:text-slate-400"><%= item.share_pct || 0 %>% das entradas</div>
                          </div>
                          <div class="text-sm font-black text-emerald-700 dark:text-emerald-300"><%= formatBRLFromCents(item.total_cents || 0) %></div>
                        </div>
                        <div class="mt-3 h-2 rounded-full bg-emerald-100 dark:bg-emerald-950/40"><div class="h-2 rounded-full bg-emerald-500 dark:bg-emerald-400" style="width:<%= item.intensity_pct || 0 %>%"></div></div>
                      </div>
                    <% }) %>
                  </div>
                <% } else { %>
                  <div class="mt-4 rounded-[24px] border border-dashed border-slate-300 bg-slate-50 px-6 py-8 text-center text-sm font-semibold leading-6 text-slate-500 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-300">Ainda não pintou entrada por aqui neste mês.</div>
                <% } %>
              </div>

              <div class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
                <div class="flex items-start justify-between gap-3">
                  <div>
                    <h3 class="text-lg font-black text-slate-900 dark:text-white">O que mais puxou por fora</h3>
                    <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Bom para ver rapidinho o que saiu sem passar pelo cartão.</p>
                  </div>
                  <span class="inline-flex items-center rounded-full border border-rose-200 bg-rose-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-rose-700 dark:border-rose-900/60 dark:bg-rose-950/20 dark:text-rose-300"><%= financeExpenseSources.length %> saída<%= financeExpenseSources.length === 1 ? '' : 's' %></span>
                </div>
                <% if (financeExpenseSources.length > 0) { %>
                  <div class="mt-4 space-y-3">
                    <% financeExpenseSources.slice(0, 6).forEach((item) => { %>
                      <div class="rounded-2xl border border-slate-200 bg-slate-50/80 px-4 py-3 dark:border-slate-700 dark:bg-slate-800/60">
                        <div class="flex items-center justify-between gap-3">
                          <div class="min-w-0">
                            <div class="truncate text-sm font-black text-slate-900 dark:text-white"><%= item.label %></div>
                            <div class="mt-1 text-xs font-semibold text-slate-500 dark:text-slate-400"><%= item.share_pct || 0 %>% das saídas por fora</div>
                          </div>
                          <div class="text-sm font-black text-rose-700 dark:text-rose-300"><%= formatBRLFromCents(item.total_cents || 0) %></div>
                        </div>
                        <div class="mt-3 h-2 rounded-full bg-rose-100 dark:bg-rose-950/40"><div class="h-2 rounded-full bg-rose-500 dark:bg-rose-400" style="width:<%= item.intensity_pct || 0 %>%"></div></div>
                      </div>
                    <% }) %>
                  </div>
                <% } else { %>
                  <div class="mt-4 rounded-[24px] border border-dashed border-slate-300 bg-slate-50 px-6 py-8 text-center text-sm font-semibold leading-6 text-slate-500 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-300">Ainda não pintou saída por fora neste mês.</div>
                <% } %>
              </div>
            </section>
          </div>
        <% } else { %>
          <div class="mt-6 rounded-[26px] border border-dashed border-slate-300 bg-slate-50 px-6 py-10 text-center dark:border-slate-700 dark:bg-slate-800/60">
            <div class="text-xl font-black text-slate-900 dark:text-white">Essa parte ainda está quietinha</div>
            <p class="mt-2 text-sm leading-6 text-slate-500 dark:text-slate-300">Assim que você tiver entradas, saídas por fora ou valor dividido no cartão, essa leitura ganha vida.</p>
            <a href="/detalhamento/<%= year %>/<%= month %>" class="op-btn op-btn-export op-btn--compact whitespace-nowrap mt-5">Abrir entradas e saídas</a>
          </div>
        <% } %>
      </div>
      <div data-op-tab-panel="split" hidden>
        <div class="grid gap-3 lg:grid-cols-4">
          <div class="rounded-[24px] border border-slate-200 bg-white/90 p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/50">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-500 dark:text-slate-400">Dividido entre pessoas</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-slate-900 dark:text-white"><%= formatBRLFromCents(totalKpis.totalPeopleAssignedCents || 0) %></div>
            <p class="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400">Tudo que já está repartido dentro das compras do mês.</p>
          </div>
          <div class="rounded-[24px] border border-amber-200 bg-amber-50/90 p-4 dark:border-amber-900/60 dark:bg-amber-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-amber-700 dark:text-amber-300">Sem dono ainda</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-amber-900 dark:text-amber-100"><%= formatBRLFromCents(totalKpis.totalUnassignedCents || 0) %></div>
            <p class="mt-2 text-xs leading-5 text-amber-800/80 dark:text-amber-100/80">Bom para achar o que ainda não foi distribuído.</p>
          </div>
          <div class="rounded-[24px] border border-emerald-200 bg-emerald-50/90 p-4 dark:border-emerald-900/60 dark:bg-emerald-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-emerald-700 dark:text-emerald-300">Pago nos cartões</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-emerald-800 dark:text-emerald-100"><%= formatBRLFromCents(totalKpis.totalPaidCardsCents || 0) %></div>
            <p class="mt-2 text-xs leading-5 text-emerald-800/80 dark:text-emerald-100/80">Parte já marcada como quitada no fechamento.</p>
          </div>
          <div class="rounded-[24px] border border-violet-200 bg-violet-50/90 p-4 dark:border-violet-900/60 dark:bg-violet-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-violet-700 dark:text-violet-300">Pessoas na roda</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-violet-900 dark:text-violet-100"><%= peopleRows.length %></div>
            <p class="mt-2 text-xs leading-5 text-violet-800/80 dark:text-violet-100/80">Quem mais puxou o mês aparece logo abaixo, junto dos cartões.</p>
          </div>
        </div>

        <div class="mt-6 grid gap-4 xl:grid-cols-2">
          <section class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
            <div class="flex items-start justify-between gap-3">
              <div>
                <h3 class="text-lg font-black text-slate-900 dark:text-white">Quem mais puxou valor</h3>
                <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Ajuda a entender a divisão do mês pelas pessoas que entraram na conta.</p>
              </div>
              <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Pessoas</span>
            </div>
            <% if (peopleRows.length > 0) { %>
              <div class="mt-4 space-y-3">
                <% peopleRows.slice(0, 6).forEach((person) => { %>
                  <a href="/month/<%= year %>/<%= month %>?f_person=<%= person.id %>" class="block rounded-2xl border border-slate-200 bg-slate-50/80 px-4 py-3 transition hover:-translate-y-0.5 hover:border-violet-300 hover:bg-violet-50/70 dark:border-slate-700 dark:bg-slate-800/60 dark:hover:border-violet-800 dark:hover:bg-violet-900/20">
                    <div class="flex items-center justify-between gap-3">
                      <div class="min-w-0">
                        <div class="truncate text-sm font-black text-slate-900 dark:text-white"><%= person.label %></div>
                        <div class="mt-1 text-xs font-semibold text-slate-500 dark:text-slate-400"><%= person.share_pct || 0 %>% do mês · já pagou <%= formatBRLFromCents(person.paid_cents || 0) %></div>
                      </div>
                      <div class="text-right">
                        <div class="text-sm font-black text-slate-900 dark:text-white"><%= formatBRLFromCents(person.total_cents || 0) %></div>
                        <div class="mt-1 text-xs font-semibold <%= Number(person.remaining_cents || 0) > 0 ? 'text-amber-700 dark:text-amber-300' : 'text-emerald-700 dark:text-emerald-300' %>"><%= Number(person.remaining_cents || 0) > 0 ? `faltam ${formatBRLFromCents(person.remaining_cents || 0)}` : 'já está zerado' %></div>
                      </div>
                    </div>
                    <div class="mt-3 h-2 rounded-full bg-violet-100 dark:bg-violet-950/40"><div class="h-2 rounded-full bg-violet-500 dark:bg-violet-400" style="width:<%= person.intensity_pct || 0 %>%"></div></div>
                  </a>
                <% }) %>
              </div>
            <% } else { %>
              <div class="mt-4 rounded-[24px] border border-dashed border-slate-300 bg-slate-50 px-6 py-8 text-center text-sm font-semibold leading-6 text-slate-500 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-300">Quando o mês tiver divisão entre pessoas, esse ranking aparece por aqui.</div>
            <% } %>
          </section>

          <section class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
            <div class="flex items-start justify-between gap-3">
              <div>
                <h3 class="text-lg font-black text-slate-900 dark:text-white">Cartões que mais carregaram o mês</h3>
                <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Mostra onde o total do mês ficou concentrado e o que ainda está pendente.</p>
              </div>
              <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Cartões</span>
            </div>
            <% if (cardRows.length > 0) { %>
              <div class="mt-4 space-y-3">
                <% cardRows.slice(0, 6).forEach((card) => { %>
                  <a href="/month/<%= year %>/<%= month %>?f_card=<%= encodeURIComponent(card.label) %>" class="block rounded-2xl border border-slate-200 bg-slate-50/80 px-4 py-3 transition hover:-translate-y-0.5 hover:border-sky-300 hover:bg-sky-50/70 dark:border-slate-700 dark:bg-slate-800/60 dark:hover:border-sky-800 dark:hover:bg-sky-900/20">
                    <div class="flex items-center justify-between gap-3">
                      <div class="min-w-0">
                        <div class="truncate text-sm font-black text-slate-900 dark:text-white"><%= card.label %></div>
                        <div class="mt-1 text-xs font-semibold text-slate-500 dark:text-slate-400"><%= card.share_pct || 0 %>% do mês · pago <%= formatBRLFromCents(card.paid_cents || 0) %></div>
                      </div>
                      <div class="text-right">
                        <div class="text-sm font-black text-slate-900 dark:text-white"><%= formatBRLFromCents(card.total_cents || 0) %></div>
                        <div class="mt-1 text-xs font-semibold <%= Number(card.remaining_cents || 0) > 0 ? 'text-amber-700 dark:text-amber-300' : 'text-emerald-700 dark:text-emerald-300' %>"><%= Number(card.remaining_cents || 0) > 0 ? `faltam ${formatBRLFromCents(card.remaining_cents || 0)}` : 'sem pendência' %></div>
                      </div>
                    </div>
                    <div class="mt-3 h-2 rounded-full bg-sky-100 dark:bg-sky-950/40"><div class="h-2 rounded-full bg-sky-500 dark:bg-sky-400" style="width:<%= card.intensity_pct || 0 %>%"></div></div>
                  </a>
                <% }) %>
              </div>
            <% } else { %>
              <div class="mt-4 rounded-[24px] border border-dashed border-slate-300 bg-slate-50 px-6 py-8 text-center text-sm font-semibold leading-6 text-slate-500 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-300">Quando esse mês ganhar compras em cartão, esse cantinho entra em cena.</div>
            <% } %>
          </section>
        </div>

        <section class="mt-6 rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
          <div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between">
            <div>
              <h3 class="text-lg font-black text-slate-900 dark:text-white">Cartão por cartão, mês a mês</h3>
              <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Aqui o papo é total de cartão mesmo. Entrou cartão novo com compra, ele aparece sozinho por aqui.</p>
            </div>
            <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Últimos 6 meses</span>
          </div>
          <% if (cardTrendByCard.length > 0) { %>
            <div class="mt-4 grid gap-4 xl:grid-cols-2">
              <% cardTrendByCard.forEach((cardTrend) => { const cardPeak = Math.max(1, Number(cardTrend.peak_cents || 0)); %>
                <section class="rounded-[24px] border border-slate-200 bg-slate-50/80 p-4 dark:border-slate-700 dark:bg-slate-800/60">
                  <div class="flex items-start justify-between gap-3">
                    <div>
                      <h4 class="text-base font-black text-slate-900 dark:text-white"><%= cardTrend.label %></h4>
                      <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Pra sacar rapidinho quando esse cartão engrossou ou deu uma folga.</p>
                    </div>
                    <span class="inline-flex items-center rounded-full border border-sky-200 bg-sky-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.18em] text-sky-700 dark:border-sky-900/60 dark:bg-sky-950/20 dark:text-sky-300"><%= formatBRLFromCents(cardTrend.current_cents || 0) %></span>
                  </div>
                  <div class="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">
                    <% cardTrend.series.forEach((point) => { const pointPct = cardPeak > 0 ? Math.max(8, Math.round((Number(point.total_cents || 0) / cardPeak) * 100)) : 8; %>
                      <a href="/month/<%= point.year %>/<%= point.month %>?f_card=<%= encodeURIComponent(cardTrend.label) %>" class="block rounded-[20px] border <%= point.is_current ? 'border-sky-300 bg-sky-50/80 dark:border-sky-800 dark:bg-sky-900/20' : 'border-slate-200 bg-white/80 dark:border-slate-700 dark:bg-slate-900/50' %> px-4 py-3 transition hover:-translate-y-0.5 hover:border-sky-300 hover:bg-sky-50/80 dark:hover:border-sky-700 dark:hover:bg-sky-900/20">
                        <div class="flex items-center justify-between gap-3">
                          <div class="text-[11px] font-bold uppercase tracking-[0.18em] <%= point.is_current ? 'text-sky-700 dark:text-sky-300' : 'text-slate-500 dark:text-slate-400' %>"><%= point.label %></div>
                          <div class="text-sm font-black text-slate-900 dark:text-white"><%= formatBRLFromCents(point.total_cents || 0) %></div>
                        </div>
                        <div class="mt-3 h-2 rounded-full bg-sky-100 dark:bg-sky-950/40"><div class="h-2 rounded-full bg-sky-500 dark:bg-sky-400" style="width:<%= pointPct %>%"></div></div>
                      </a>
                    <% }) %>
                  </div>
                </section>
              <% }) %>
            </div>
          <% } else { %>
            <div class="mt-4 rounded-[24px] border border-dashed border-slate-300 bg-slate-50 px-6 py-8 text-center text-sm font-semibold leading-6 text-slate-500 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-300">Assim que os cartões ganharem histórico, essa leitura entra em cena.</div>
          <% } %>
        </section>
      </div>

      <div data-op-tab-panel="places" hidden>
        <div class="grid gap-3 lg:grid-cols-4 mb-6">
          <div class="rounded-[24px] border border-slate-200 bg-white/90 p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/50 lg:col-span-2">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-500 dark:text-slate-400">Lugar campeão</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-slate-900 dark:text-white"><%= merchantTop ? merchantTop.label : 'Ainda sem campeão' %></div>
            <p class="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400"><%= merchantTop ? `${formatBRLFromCents(merchantTop.total_cents || 0)} em ${merchantTop.txn_count || 0} compra${Number(merchantTop.txn_count || 0) === 1 ? '' : 's'}.` : 'Assim que um lugar puxar mais do que os outros, ele ganha destaque aqui.' %></p>
          </div>
          <div class="rounded-[24px] border border-violet-200 bg-violet-50/90 p-4 dark:border-violet-900/60 dark:bg-violet-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-violet-700 dark:text-violet-300">Ranking</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-violet-900 dark:text-violet-100"><%= merchantRanking.length %></div>
            <p class="mt-2 text-xs leading-5 text-violet-800/80 dark:text-violet-100/80">Quantos lugares entraram no mapa deste mês.</p>
          </div>
          <div class="rounded-[24px] border border-emerald-200 bg-emerald-50/90 p-4 dark:border-emerald-900/60 dark:bg-emerald-950/20">
            <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-emerald-700 dark:text-emerald-300">Sugestões</div>
            <div class="mt-2 text-2xl font-black tracking-tight text-emerald-900 dark:text-emerald-100"><%= merchantSuggestions.length %></div>
            <p class="mt-2 text-xs leading-5 text-emerald-800/80 dark:text-emerald-100/80">Nomes que o app separou para você confirmar.</p>
          </div>
        </div>

<section class="mb-8 rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40" data-summary-ranking-root>
        <div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between">
          <div>
            <h3 class="text-lg font-black text-slate-900 dark:text-white">Ranking dos lugares</h3>
            <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Aqui fica o pódio do mês: quem mais apareceu, quem puxou mais grana e quem deu uma mudada em relação ao mês passado.</p>
          </div>
          <div class="flex flex-wrap items-center gap-2 sm:justify-end">
            <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Leitura por nome</span>
            <button type="button" class="inline-flex items-center justify-center rounded-2xl border border-slate-200 bg-white px-4 py-2 text-sm font-bold text-slate-700 transition hover:-translate-y-0.5 hover:border-slate-300 hover:bg-slate-50 dark:border-slate-600 dark:bg-slate-900 dark:text-slate-100 dark:hover:bg-slate-800" data-summary-ranking-toggle aria-expanded="<%= merchantRankingDefaultOpen ? 'true' : 'false' %>">
              <span data-summary-ranking-toggle-label><%= merchantRankingDefaultOpen ? 'Esconder ranking' : 'Mostrar ranking' %></span>
            </button>
          </div>
        </div>

        <div class="mt-4" data-summary-ranking-panel <%= merchantRankingDefaultOpen ? '' : 'hidden' %>>
        <% if (merchantRanking.length > 0) { %>
          <% const merchantMaxTotal = merchantRanking.reduce((best, item) => Math.max(best, Number(item.total_cents || 0)), 0); %>
          <% const rankingPodium = merchantRanking.slice(0, 3); %>
          <% const rankingRest = merchantRanking.slice(3); %>
          <% const medalMeta = {
            1: { emoji: '🥇', label: 'Ouro do mês', border: 'border-amber-200 dark:border-amber-700', bg: 'bg-amber-50/90 dark:bg-amber-900/20', chip: 'bg-amber-100 text-amber-800 dark:bg-amber-900/40 dark:text-amber-100' },
            2: { emoji: '🥈', label: 'Prata valente', border: 'border-slate-300 dark:border-slate-600', bg: 'bg-slate-50/90 dark:bg-slate-800/80', chip: 'bg-slate-200 text-slate-700 dark:bg-slate-700 dark:text-slate-100' },
            3: { emoji: '🥉', label: 'Bronze na cola', border: 'border-orange-200 dark:border-orange-700', bg: 'bg-orange-50/90 dark:bg-orange-900/20', chip: 'bg-orange-100 text-orange-800 dark:bg-orange-900/40 dark:text-orange-100' }
          }; %>
          <% const rankingLeader = merchantRanking[0]; %>

          <div class="rounded-[24px] border border-amber-200 bg-gradient-to-br from-amber-50 via-white to-emerald-50 p-5 shadow-sm dark:border-amber-800 dark:from-amber-900/25 dark:via-slate-900 dark:to-emerald-900/20">
            <div class="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between">
              <div class="min-w-0 flex-1">
                <div class="inline-flex items-center gap-2 rounded-full bg-amber-100 px-3 py-1 text-[11px] font-black uppercase tracking-[0.22em] text-amber-800 dark:bg-amber-900/50 dark:text-amber-100">🏆 Campeão do mês</div>
                <div class="mt-3 flex flex-wrap items-center gap-2">
                  <h4 class="text-2xl font-black tracking-tight text-slate-900 dark:text-white"><%= rankingLeader.label %></h4>
                  <span class="inline-flex items-center rounded-full border border-amber-200 bg-white/80 px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.18em] text-amber-700 dark:border-amber-700 dark:bg-slate-900/60 dark:text-amber-200"><%= rankingLeader.share_pct || 0 %>% do mês</span>
                </div>
                <p class="mt-2 max-w-2xl text-sm leading-6 text-slate-600 dark:text-slate-300">Esse aqui foi o campeão da vez. Se bater curiosidade, é só tocar no card dele logo abaixo.</p>
                <div class="mt-3 flex flex-wrap items-center gap-2 text-xs font-semibold text-slate-500 dark:text-slate-400">
                  <span><%= rankingLeader.txn_count || 0 %> compra<%= Number(rankingLeader.txn_count || 0) === 1 ? '' : 's' %></span>
                  <span class="hidden sm:inline">·</span>
                  <span>ticket médio de <%= formatBRLFromCents(rankingLeader.avg_ticket_cents || 0) %></span>
                  <% if (rankingLeader.delta_pct !== null && rankingLeader.delta_pct !== undefined) { %>
                    <span class="hidden sm:inline">·</span>
                    <span class="font-bold <%= Number(rankingLeader.delta_cents || 0) > 0 ? 'text-rose-600 dark:text-rose-300' : Number(rankingLeader.delta_cents || 0) < 0 ? 'text-emerald-700 dark:text-emerald-300' : 'text-slate-600 dark:text-slate-300' %>"><%= Number(rankingLeader.delta_cents || 0) > 0 ? 'Subiu' : Number(rankingLeader.delta_cents || 0) < 0 ? 'Respirou' : 'Ficou estável' %> <%= Math.abs(Number(rankingLeader.delta_pct || 0)) %>% em relação ao mês passado</span>
                  <% } else { %>
                    <span class="hidden sm:inline">·</span>
                    <span class="font-bold text-slate-600 dark:text-slate-300">Primeira aparição forte por aqui</span>
                  <% } %>
                </div>
              </div>
              <div class="rounded-[22px] border border-white/70 bg-white/80 px-5 py-4 text-left shadow-sm dark:border-slate-700 dark:bg-slate-900/60 lg:min-w-[220px] lg:text-right">
                <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-500 dark:text-slate-400">Total no mês</div>
                <div class="mt-2 text-3xl font-black tracking-tight text-slate-900 dark:text-white"><%= formatBRLFromCents(rankingLeader.total_cents || 0) %></div>
                <div class="mt-2 text-xs font-semibold <%= Number(rankingLeader.delta_cents || 0) > 0 ? 'text-rose-600 dark:text-rose-300' : Number(rankingLeader.delta_cents || 0) < 0 ? 'text-emerald-700 dark:text-emerald-300' : 'text-slate-500 dark:text-slate-400' %>">
                  <% if (rankingLeader.delta_pct !== null && rankingLeader.delta_pct !== undefined) { %>
                    <%= Number(rankingLeader.delta_cents || 0) > 0 ? '▲' : Number(rankingLeader.delta_cents || 0) < 0 ? '▼' : '•' %>
                    <%= Number(rankingLeader.delta_cents || 0) > 0 ? '+' : '' %><%= formatBRLFromCents(rankingLeader.delta_cents || 0) %> em relação ao mês passado
                  <% } else { %>
                    Ainda sem chão para comparar esse craque.
                  <% } %>
                </div>
              </div>
            </div>
          </div>

          <div class="mt-4 grid gap-3 lg:grid-cols-3">
            <% rankingPodium.forEach((merchant) => { %>
              <% const merchantHref = merchant.search_term ? `/month/${year}/${month}?${new URLSearchParams({ f_desc: String(merchant.search_term || '').trim() }).toString()}` : null; %>
              <% const medal = medalMeta[merchant.position] || medalMeta[3]; %>
              <% const merchantSample = merchant.sample_descriptions && merchant.sample_descriptions.length ? merchant.sample_descriptions[0] : ''; %>
              <% const cardClass = `group rounded-[24px] border p-4 transition duration-200 ${medal.border} ${medal.bg} ${merchantHref ? 'block hover:-translate-y-0.5 hover:shadow-md focus:outline-none focus:ring-2 focus:ring-emerald-400' : 'block'}`; %>
              <% if (merchantHref) { %>
              <a href="<%= merchantHref %>" class="<%= cardClass %>" aria-label="Abrir compras de <%= merchant.label %>">
              <% } else { %>
              <div class="<%= cardClass %>">
              <% } %>
                <div class="flex items-start justify-between gap-3">
                  <div class="min-w-0 flex-1">
                    <div class="inline-flex items-center gap-2 rounded-full px-3 py-1 text-[11px] font-black uppercase tracking-[0.2em] <%= medal.chip %>">
                      <span><%= medal.emoji %></span>
                      <span><%= medal.label %></span>
                    </div>
                    <div class="mt-3 truncate text-lg font-black text-slate-900 dark:text-white"><%= merchant.label %></div>
                  </div>
                  <span class="inline-flex h-9 min-w-[2.25rem] items-center justify-center rounded-full bg-slate-900 px-2 text-sm font-black text-white dark:bg-slate-100 dark:text-slate-900">#<%= merchant.position %></span>
                </div>
                <div class="mt-4 text-2xl font-black tracking-tight text-slate-900 dark:text-white"><%= formatBRLFromCents(merchant.total_cents || 0) %></div>
                <div class="mt-1 text-xs font-semibold text-slate-500 dark:text-slate-400"><%= merchant.share_pct || 0 %>% do mês · <%= merchant.txn_count || 0 %> compra<%= Number(merchant.txn_count || 0) === 1 ? '' : 's' %></div>
                <div class="mt-3 flex flex-wrap items-center gap-2 text-xs font-semibold">
                  <span class="inline-flex items-center rounded-full border border-white/80 bg-white/80 px-2.5 py-1 text-slate-600 dark:border-slate-700 dark:bg-slate-900/70 dark:text-slate-200">ticket médio <%= formatBRLFromCents(merchant.avg_ticket_cents || 0) %></span>
                  <% if (merchant.delta_pct !== null && merchant.delta_pct !== undefined) { %>
                    <span class="inline-flex items-center rounded-full border px-2.5 py-1 <%= Number(merchant.delta_cents || 0) > 0 ? 'border-rose-200 bg-rose-50 text-rose-700 dark:border-rose-800 dark:bg-rose-900/30 dark:text-rose-200' : Number(merchant.delta_cents || 0) < 0 ? 'border-emerald-200 bg-emerald-50 text-emerald-700 dark:border-emerald-800 dark:bg-emerald-900/30 dark:text-emerald-200' : 'border-slate-200 bg-white text-slate-600 dark:border-slate-700 dark:bg-slate-900/60 dark:text-slate-200' %>">
                      <%= Number(merchant.delta_cents || 0) > 0 ? '▲' : Number(merchant.delta_cents || 0) < 0 ? '▼' : '•' %>
                      <span class="ml-1"><%= Math.abs(Number(merchant.delta_pct || 0)) %>% em relação ao mês passado</span>
                    </span>
                  <% } else { %>
                    <span class="inline-flex items-center rounded-full border border-slate-200 bg-white px-2.5 py-1 text-slate-600 dark:border-slate-700 dark:bg-slate-900/60 dark:text-slate-200">apareceu agora</span>
                  <% } %>
                </div>
                <% if (merchantSample) { %>
                  <p class="mt-3 text-xs leading-5 text-slate-500 dark:text-slate-400">Exemplo lido: <span class="font-semibold text-slate-700 dark:text-slate-200"><%= merchantSample %></span></p>
                <% } %>
                <div class="mt-3 text-[11px] font-bold uppercase tracking-[0.18em] text-emerald-700 opacity-0 transition group-hover:opacity-100 dark:text-emerald-300"><%= merchantHref ? 'Toque para abrir as compras desse nome' : 'Só para olhar' %></div>
              <% if (merchantHref) { %>
              </a>
              <% } else { %>
              </div>
              <% } %>
            <% }) %>
          </div>

          <% if (rankingRest.length > 0) { %>
          <div class="mt-4 space-y-3">
            <% rankingRest.forEach((merchant) => { %>
              <% const widthPct = merchantMaxTotal > 0 ? Math.max(8, Math.round((Number(merchant.total_cents || 0) / merchantMaxTotal) * 100)) : 8; %>
              <% const merchantHref = merchant.search_term ? `/month/${year}/${month}?${new URLSearchParams({ f_desc: String(merchant.search_term || '').trim() }).toString()}` : null; %>
              <% const merchantSample = merchant.sample_descriptions && merchant.sample_descriptions.length ? merchant.sample_descriptions[0] : ''; %>
              <% if (merchantHref) { %>
              <a href="<%= merchantHref %>" class="group block rounded-[22px] border border-slate-200 bg-slate-50/80 px-4 py-4 transition duration-200 hover:-translate-y-0.5 hover:border-emerald-300 hover:bg-emerald-50/70 focus:outline-none focus:ring-2 focus:ring-emerald-400 dark:border-slate-700 dark:bg-slate-800/70 dark:hover:border-emerald-700 dark:hover:bg-emerald-900/10" aria-label="Abrir compras de <%= merchant.label %>">
              <% } else { %>
              <div class="group block rounded-[22px] border border-slate-200 bg-slate-50/80 px-4 py-4 transition duration-200 dark:border-slate-700 dark:bg-slate-800/70">
              <% } %>
                <div class="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between">
                  <div class="min-w-0 flex-1">
                    <div class="flex flex-wrap items-center gap-2">
                      <span class="inline-flex h-8 min-w-[2rem] items-center justify-center rounded-full bg-slate-900 px-2 text-xs font-black text-white dark:bg-slate-100 dark:text-slate-900">#<%= merchant.position %></span>
                      <div class="truncate text-base font-black text-slate-900 dark:text-white"><%= merchant.label %></div>
                      <span class="inline-flex items-center rounded-full border border-slate-200 bg-white px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.18em] text-slate-500 dark:border-slate-700 dark:bg-slate-900/60 dark:text-slate-300"><%= merchant.share_pct || 0 %>% do mês</span>
                    </div>
                    <div class="mt-2 flex flex-wrap items-center gap-2 text-xs font-medium leading-5 text-slate-500 dark:text-slate-400">
                      <span><%= merchant.txn_count || 0 %> compra<%= Number(merchant.txn_count || 0) === 1 ? '' : 's' %></span>
                      <span class="hidden sm:inline">·</span>
                      <span>ticket médio de <%= formatBRLFromCents(merchant.avg_ticket_cents || 0) %></span>
                      <% if (merchant.delta_pct !== null && merchant.delta_pct !== undefined) { %>
                        <span class="hidden sm:inline">·</span>
                        <span class="font-bold <%= Number(merchant.delta_cents || 0) > 0 ? 'text-rose-600 dark:text-rose-300' : Number(merchant.delta_cents || 0) < 0 ? 'text-emerald-700 dark:text-emerald-300' : 'text-slate-600 dark:text-slate-300' %>">
                          <%= Number(merchant.delta_cents || 0) > 0 ? 'Subiu' : Number(merchant.delta_cents || 0) < 0 ? 'Respirou' : 'Ficou estável' %> <%= Math.abs(Number(merchant.delta_pct || 0)) %>%
                        </span>
                      <% } else { %>
                        <span class="hidden sm:inline">·</span>
                        <span class="font-bold text-slate-600 dark:text-slate-300">apareceu agora</span>
                      <% } %>
                      <% if (merchant.confidence === 'high') { %>
                        <span class="hidden sm:inline">·</span>
                        <span class="font-bold text-emerald-700 dark:text-emerald-300">nome certinho</span>
                      <% } %>
                    </div>
                    <% if (merchantSample) { %>
                      <p class="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400">Exemplo lido: <span class="font-semibold text-slate-700 dark:text-slate-200"><%= merchantSample %></span></p>
                    <% } %>
                    <div class="mt-3 h-2.5 overflow-hidden rounded-full bg-slate-200/80 dark:bg-slate-700/80">
                      <div class="h-full rounded-full bg-gradient-to-r from-emerald-400 via-emerald-500 to-teal-500" style="width: <%= widthPct %>%"></div>
                    </div>
                  </div>
                  <div class="text-left lg:ml-4 lg:min-w-[160px] lg:text-right">
                    <div class="text-lg font-black tracking-tight text-slate-900 dark:text-white"><%= formatBRLFromCents(merchant.total_cents || 0) %></div>
                    <div class="mt-1 text-[11px] font-bold uppercase tracking-[0.18em] <%= Number(merchant.delta_cents || 0) > 0 ? 'text-rose-600 dark:text-rose-300' : Number(merchant.delta_cents || 0) < 0 ? 'text-emerald-700 dark:text-emerald-300' : 'text-slate-500 dark:text-slate-400' %>">
                      <% if (merchant.delta_pct !== null && merchant.delta_pct !== undefined) { %>
                        <%= Number(merchant.delta_cents || 0) > 0 ? '▲' : Number(merchant.delta_cents || 0) < 0 ? '▼' : '•' %> <%= Number(merchant.delta_cents || 0) > 0 ? '+' : '' %><%= formatBRLFromCents(merchant.delta_cents || 0) %>
                      <% } else { %>
                        primeira aparição forte
                      <% } %>
                    </div>
                    <div class="mt-1 text-[11px] font-bold uppercase tracking-[0.18em] text-emerald-700 opacity-0 transition group-hover:opacity-100 dark:text-emerald-300"><%= merchantHref ? 'Abrir compras desse nome' : 'Só para olhar' %></div>
                  </div>
                </div>
              <% if (merchantHref) { %>
              </a>
              <% } else { %>
              </div>
              <% } %>
            <% }) %>
          </div>
          <% } %>
        <% } else { %>
          <div class="mt-4 flex min-h-[180px] items-center justify-center rounded-[24px] border border-dashed border-slate-300 bg-slate-50 px-6 py-10 text-center text-sm font-semibold leading-6 text-slate-500 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-300">Ainda não apareceu compra suficiente para montar esse ranking. Assim que o mês ganhar movimento, essa área entra em cena.</div>
        <% } %>
        </div>
    </section>

<section class="mb-8 op-surface-card rounded-[28px] border border-slate-200 bg-white/95 p-4 shadow-sm dark:border-slate-700 dark:bg-slate-800/90 lg:p-6" data-summary-intel-root>
      <div class="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between">
        <div>
          <div class="inline-flex items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.24em] text-emerald-700 dark:border-emerald-800 dark:bg-emerald-900/20 dark:text-emerald-200">Ajuda com nomes</div>
          <h2 class="mt-3 text-2xl font-black tracking-tight text-slate-900 dark:text-white">Como eu pego o jeito dos nomes que aparecem</h2>
          <p class="mt-2 max-w-3xl text-sm leading-6 text-slate-600 dark:text-slate-300">Quando você confirma um nome, eu vou pegando o jeito e deixo o ranking lá de cima mais redondinho. Sem inventar moda sozinho por aqui.</p>
        </div>
        <div class="flex flex-wrap items-center gap-2 lg:justify-end">
          <button type="button" class="inline-flex items-center justify-center rounded-2xl bg-slate-900 px-4 py-2 text-sm font-bold text-white transition hover:-translate-y-0.5 hover:bg-slate-800 dark:bg-slate-100 dark:text-slate-900 dark:hover:bg-white" data-summary-intel-toggle aria-expanded="<%= merchantSectionDefaultOpen ? 'true' : 'false' %>">
            <span data-summary-intel-toggle-label><%= merchantSectionDefaultOpen ? 'Esconder ajuda com nomes' : 'Mostrar ajuda com nomes' %></span>
          </button>
        </div>
      </div>

      <div class="mt-5 space-y-5" data-summary-intel-panel <%= merchantSectionDefaultOpen ? '' : 'hidden' %>>
        <div class="rounded-[24px] border border-slate-200 bg-slate-50/80 px-4 py-3 text-sm text-slate-600 dark:border-slate-700 dark:bg-slate-900/40 dark:text-slate-300 lg:max-w-sm">
          <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-slate-500 dark:text-slate-400">Como isso entra nos eixos</div>
          <p class="mt-2 leading-6">Você confirma quando fizer sentido, eu aprendo com calma e vou deixando os nomes mais arrumadinhos. Se não curtir, é só pausar e pronto.</p>
        </div>

      <div class="mt-5 grid gap-3 lg:grid-cols-3">
        <div class="rounded-[24px] border border-emerald-200 bg-emerald-50/90 p-4 dark:border-emerald-900/60 dark:bg-emerald-950/20">
          <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-emerald-700 dark:text-emerald-300">Destaque do mês</div>
          <div class="mt-2 text-xl font-black tracking-tight text-emerald-900 dark:text-emerald-100">
            <% if (merchantTop) { %>
              Você gastou <%= formatBRLFromCents(merchantTop.total_cents || 0) %> em <%= merchantTop.label %>
            <% } else { %>
              Ainda sem um campeão do mês
            <% } %>
          </div>
          <p class="mt-2 text-xs leading-5 text-emerald-800/80 dark:text-emerald-100/80">
            <% if (merchantTop) { %>
              Foram <span class="font-bold"><%= merchantTop.txn_count || 0 %></span> compra(s), puxando <span class="font-bold"><%= merchantTop.share_pct || 0 %>%</span> do total lido por estabelecimento. Aqui a lupa já entrega o protagonista da novela.
            <% } else { %>
              Assim que o mês ganhar compras, esse bloco mostra quem mais apareceu por aqui.
            <% } %>
          </p>
        </div>
        <div class="rounded-[24px] border border-violet-200 bg-violet-50/90 p-4 dark:border-violet-900/60 dark:bg-violet-950/20">
          <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-violet-700 dark:text-violet-300">Nomes já reconhecidos</div>
          <div class="mt-2 text-xl font-black tracking-tight text-violet-900 dark:text-violet-100"><%= Number(graphInsights?.merchantCount || 0) %> nome<%= Number(graphInsights?.merchantCount || 0) === 1 ? '' : 's' %> no mapa</div>
          <p class="mt-2 text-xs leading-5 text-violet-800/80 dark:text-violet-100/80">Dessas, <span class="font-bold"><%= Number(graphInsights?.recognizedMerchantCount || 0) %></span> vieram de agrupamentos mais certeiros por palavra-chave. <% if (Number(graphInsights?.learnedMerchantCount || 0) > 0) { %><span class="font-bold"><%= Number(graphInsights?.learnedMerchantCount || 0) %></span> já aprenderam com as suas confirmações.<% } else { %>O resto entrou no modo prudente, sem forçar apelido onde não precisava.<% } %></p>
        </div>
        <div class="rounded-[24px] border border-amber-200 bg-amber-50/90 p-4 dark:border-amber-900/60 dark:bg-amber-950/20">
          <div class="text-[11px] font-bold uppercase tracking-[0.22em] text-amber-700 dark:text-amber-300">Como isso ajuda</div>
          <div class="mt-2 text-xl font-black tracking-tight text-amber-900 dark:text-amber-100">Ranking que já abre a investigação</div>
          <p class="mt-2 text-xs leading-5 text-amber-800/80 dark:text-amber-100/80">Tocou num estabelecimento? A gente abre o mês já com a descrição filtrada, para você investigar sem ficar pescando compra na unha.</p>
        </div>
      </div>

      <div class="mt-5 grid gap-3 xl:grid-cols-[1.3fr,0.9fr]">
        <div class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
          <div class="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between">
            <div>
              <h3 class="text-lg font-black text-slate-900 dark:text-white">Memória que você ensinou pro app</h3>
              <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Cada confirmação sua vira memória boa. Cada pausa evita teimosia. O app aprende, mas continua te ouvindo.</p>
            </div>
            <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Aprende com você</span>
          </div>

          <div class="mt-4 grid gap-3 md:grid-cols-3">
            <div class="rounded-2xl border border-emerald-200 bg-emerald-50/80 px-4 py-3 dark:border-emerald-900/50 dark:bg-emerald-950/20">
              <div class="text-[11px] font-bold uppercase tracking-[0.18em] text-emerald-700 dark:text-emerald-300">Memórias ativas</div>
              <div class="mt-2 text-2xl font-black text-emerald-900 dark:text-emerald-100"><%= Number(merchantLearning?.activeCount || 0) %></div>
              <p class="mt-1 text-xs leading-5 text-emerald-800/80 dark:text-emerald-100/80">Nomes que você já aprovou e que podem aparecer certinhos nas próximas compras.</p>
            </div>
            <div class="rounded-2xl border border-violet-200 bg-violet-50/80 px-4 py-3 dark:border-violet-900/50 dark:bg-violet-950/20">
              <div class="text-[11px] font-bold uppercase tracking-[0.18em] text-violet-700 dark:text-violet-300">Aplicadas no mês</div>
              <div class="mt-2 text-2xl font-black text-violet-900 dark:text-violet-100"><%= Number(merchantLearning?.appliedRuleTxnCount || 0) %></div>
              <p class="mt-1 text-xs leading-5 text-violet-800/80 dark:text-violet-100/80">Compras deste mês que já aproveitaram essa memória, somando <span class="font-bold"><%= formatBRLFromCents(merchantLearning?.appliedRuleTotalCents || 0) %></span>.</p>
            </div>
            <div class="rounded-2xl border border-amber-200 bg-amber-50/80 px-4 py-3 dark:border-amber-900/50 dark:bg-amber-950/20">
              <div class="text-[11px] font-bold uppercase tracking-[0.18em] text-amber-700 dark:text-amber-300">Sugestões na fila</div>
              <div class="mt-2 text-2xl font-black text-amber-900 dark:text-amber-100"><%= Number(graphInsights?.merchantSuggestionCount || 0) %></div>
              <p class="mt-1 text-xs leading-5 text-amber-800/80 dark:text-amber-100/80">Quando aparecer nome repetido com cara de conhecido, ele vem aqui pedir seu ok.</p>
            </div>
          </div>

          <% if (merchantLearning?.mostAppliedRule) { %>
            <div class="mt-4 rounded-[22px] border border-slate-200 bg-slate-50/80 px-4 py-4 dark:border-slate-700 dark:bg-slate-800/70">
              <div class="text-[11px] font-bold uppercase tracking-[0.18em] text-slate-500 dark:text-slate-400">Memória mais acionada</div>
              <div class="mt-2 text-lg font-black text-slate-900 dark:text-white"><%= merchantLearning.mostAppliedRule.label %></div>
              <p class="mt-1 text-sm leading-6 text-slate-600 dark:text-slate-300">Esse nome apareceu em <span class="font-bold"><%= Number(merchantLearning.mostAppliedRule.applied_txn_count || 0) %></span> compra(s) do mês e puxou <span class="font-bold"><%= formatBRLFromCents(merchantLearning.mostAppliedRule.applied_total_cents || 0) %></span>. Tá rendendo mais que café em segunda.</p>
            </div>
          <% } %>
        </div>

        <div class="rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
          <div class="flex items-start justify-between gap-3">
            <div>
              <h3 class="text-lg font-black text-slate-900 dark:text-white">O que o app já pegou no ar</h3>
              <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Aqui fica o que já foi aprendido com a sua ajuda.</p>
            </div>
            <span class="inline-flex items-center rounded-full border border-slate-200 bg-slate-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-300">Você no comando</span>
          </div>

          <% if (merchantLearning?.activeRules && merchantLearning.activeRules.length > 0) { %>
            <div class="mt-4 space-y-3">
              <% merchantLearning.activeRules.forEach((rule) => { %>
                <div class="rounded-[22px] border border-slate-200 bg-slate-50/80 px-4 py-4 dark:border-slate-700 dark:bg-slate-800/70">
                  <div class="flex flex-col gap-3 sm:flex-row sm:items-start sm:justify-between">
                    <div class="min-w-0 flex-1">
                      <div class="flex flex-wrap items-center gap-2">
                        <div class="truncate text-base font-black text-slate-900 dark:text-white"><%= rule.label %></div>
                        <span class="inline-flex items-center rounded-full border border-emerald-200 bg-emerald-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.18em] text-emerald-700 dark:border-emerald-800 dark:bg-emerald-900/20 dark:text-emerald-200">ativa</span>
                      </div>
                      <div class="mt-2 flex flex-wrap items-center gap-2 text-xs font-medium leading-5 text-slate-500 dark:text-slate-400">
                        <span>padrão: <span class="font-bold text-slate-800 dark:text-slate-100"><%= rule.normalizedPattern %></span></span>
                        <span class="hidden sm:inline">·</span>
                        <span><%= Number(rule.applied_txn_count || 0) %> compra<%= Number(rule.applied_txn_count || 0) === 1 ? '' : 's' %> no mês</span>
                      </div>
                      <% if (rule.source_sample) { %>
                        <p class="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400">Exemplo visto: <span class="font-semibold text-slate-700 dark:text-slate-200"><%= rule.source_sample %></span></p>
                      <% } %>
                    </div>
                    <div class="sm:w-auto">
                      <form method="POST" action="/analytics/<%= year %>/<%= month %>/merchant-learning/pause" data-scroll-preserve-form data-scroll-restore-target="[data-summary-intel-root]">
                        <input type="hidden" name="pattern" value="<%= rule.normalizedPattern %>">
                        <input type="hidden" name="label" value="<%= rule.label %>">
                        <input type="hidden" name="search_term" value="<%= rule.search_term || '' %>">
                        <input type="hidden" name="source_sample" value="<%= rule.source_sample || '' %>">
                        <button type="submit" class="inline-flex w-full items-center justify-center rounded-2xl border border-slate-300 bg-white px-4 py-3 text-sm font-bold text-slate-700 transition hover:-translate-y-0.5 hover:border-slate-400 hover:bg-slate-50 dark:border-slate-600 dark:bg-slate-900 dark:text-slate-200 dark:hover:bg-slate-800">Pausar aprendizado</button>
                      </form>
                    </div>
                  </div>
                </div>
              <% }) %>
            </div>
          <% } else { %>
            <div class="mt-4 rounded-[24px] border border-dashed border-slate-300 bg-slate-50 px-6 py-8 text-center text-sm font-semibold leading-6 text-slate-500 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-300">Ainda não tem nada aprendido por aqui. Quando você confirmar uma sugestão, ela passa a morar nesse cantinho.</div>
          <% } %>

          <% if (merchantLearning?.pausedRules && merchantLearning.pausedRules.length > 0) { %>
            <div class="mt-4 rounded-[22px] border border-slate-200 bg-slate-50/70 p-4 dark:border-slate-700 dark:bg-slate-800/60">
              <div class="text-[11px] font-bold uppercase tracking-[0.18em] text-slate-500 dark:text-slate-400">Na geladeira por escolha sua</div>
              <div class="mt-3 space-y-3">
                <% merchantLearning.pausedRules.forEach((rule) => { %>
                  <div class="flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white px-4 py-3 dark:border-slate-700 dark:bg-slate-900/70">
                    <div>
                      <div class="text-sm font-black text-slate-900 dark:text-white"><%= rule.label %></div>
                      <p class="mt-1 text-xs leading-5 text-slate-500 dark:text-slate-400">Padrão pausado: <span class="font-semibold text-slate-700 dark:text-slate-200"><%= rule.normalizedPattern %></span></p>
                    </div>
                    <form method="POST" action="/analytics/<%= year %>/<%= month %>/merchant-learning/resume" data-scroll-preserve-form data-scroll-restore-target="[data-summary-intel-root]">
                      <input type="hidden" name="pattern" value="<%= rule.normalizedPattern %>">
                      <input type="hidden" name="label" value="<%= rule.label %>">
                      <input type="hidden" name="search_term" value="<%= rule.search_term || '' %>">
                      <input type="hidden" name="source_sample" value="<%= rule.source_sample || '' %>">
                      <button type="submit" class="inline-flex w-full items-center justify-center rounded-2xl bg-slate-900 px-4 py-3 text-sm font-bold text-white transition hover:-translate-y-0.5 hover:bg-slate-800 dark:bg-slate-100 dark:text-slate-900 dark:hover:bg-white">Reativar aprendizado</button>
                    </form>
                  </div>
                <% }) %>
              </div>
            </div>
          <% } %>
        </div>
      </div>

      <div class="mt-5 rounded-[26px] border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900/40">
        <div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between">
          <div>
            <h3 class="text-lg font-black text-slate-900 dark:text-white">Sugestões para você confirmar</h3>
            <p class="mt-1 text-sm text-slate-500 dark:text-slate-400">Quando o app percebe um nome que se repete, ele pede seu ok antes de juntar as compras. Nada de sair batizando tudo sozinho.</p>
          </div>
          <span class="inline-flex items-center rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1 text-[11px] font-bold uppercase tracking-[0.2em] text-emerald-700 dark:border-emerald-900/60 dark:bg-emerald-950/20 dark:text-emerald-300"><%= merchantSuggestions.length %> sugest<%= merchantSuggestions.length === 1 ? 'ão' : 'ões' %> na fila</span>
        </div>

        <% if (merchantSuggestions.length > 0) { %>
          <div class="mt-4 grid gap-3 lg:grid-cols-2">
            <% merchantSuggestions.forEach((suggestion) => { %>
              <div class="rounded-[24px] border border-slate-200 bg-slate-50/80 p-4 dark:border-slate-700 dark:bg-slate-800/60">
                <div class="flex items-start justify-between gap-3">
                  <div>
                    <div class="text-[11px] font-bold uppercase tracking-[0.2em] text-slate-500 dark:text-slate-400">Sugestão #<%= suggestion.position %></div>
                    <div class="mt-2 text-lg font-black tracking-tight text-slate-900 dark:text-white"><%= suggestion.label %></div>
                    <p class="mt-1 text-xs leading-5 text-slate-500 dark:text-slate-400">Jeito que apareceu: <span class="font-bold text-slate-700 dark:text-slate-200"><%= suggestion.preview_label || suggestion.pattern %></span></p>
                  </div>
                  <span class="inline-flex items-center rounded-full border border-violet-200 bg-violet-50 px-2.5 py-1 text-[10px] font-bold uppercase tracking-[0.18em] text-violet-700 dark:border-violet-900/60 dark:bg-violet-950/30 dark:text-violet-300"><%= suggestion.txn_count %> compra<%= Number(suggestion.txn_count || 0) === 1 ? '' : 's' %></span>
                </div>
                <div class="mt-3 rounded-2xl border border-dashed border-slate-300 bg-white/80 px-3 py-3 text-xs leading-5 text-slate-600 dark:border-slate-600 dark:bg-slate-900/50 dark:text-slate-300">
                  Exemplo lido: <span class="font-semibold text-slate-800 dark:text-slate-100"><%= suggestion.sample_description || 'Sem exemplo nessa rodada' %></span>
                </div>
                <div class="mt-3 flex flex-wrap items-center gap-2 text-xs font-medium text-slate-500 dark:text-slate-400">
                  <span>Total visto: <span class="font-bold text-slate-800 dark:text-slate-100"><%= formatBRLFromCents(suggestion.total_cents || 0) %></span></span>
                  <span class="hidden sm:inline">·</span>
                  <span>Filtro pronto: <span class="font-bold text-slate-800 dark:text-slate-100"><%= suggestion.search_term || suggestion.pattern %></span></span>
                </div>
                <div class="mt-4 flex flex-col gap-2 sm:flex-row">
                  <form method="POST" action="/analytics/<%= year %>/<%= month %>/merchant-suggestions/confirm" class="flex-1" data-scroll-preserve-form data-scroll-restore-target="[data-summary-intel-root]">
                    <input type="hidden" name="pattern" value="<%= suggestion.pattern %>">
                    <input type="hidden" name="label" value="<%= suggestion.label %>">
                    <input type="hidden" name="preview_label" value="<%= suggestion.preview_label || '' %>">
                    <input type="hidden" name="search_term" value="<%= suggestion.search_term || '' %>">
                    <input type="hidden" name="sample_description" value="<%= suggestion.sample_description || '' %>">
                    <button type="submit" class="inline-flex w-full items-center justify-center rounded-2xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white transition hover:-translate-y-0.5 hover:bg-emerald-700">Pode juntar esses nomes</button>
                  </form>
                  <form method="POST" action="/analytics/<%= year %>/<%= month %>/merchant-suggestions/dismiss" class="sm:w-auto" data-scroll-preserve-form data-scroll-restore-target="[data-summary-intel-root]">
                    <input type="hidden" name="pattern" value="<%= suggestion.pattern %>">
                    <input type="hidden" name="label" value="<%= suggestion.label %>">
                    <input type="hidden" name="preview_label" value="<%= suggestion.preview_label || '' %>">
                    <input type="hidden" name="search_term" value="<%= suggestion.search_term || '' %>">
                    <input type="hidden" name="sample_description" value="<%= suggestion.sample_description || '' %>">
                    <button type="submit" class="inline-flex w-full items-center justify-center rounded-2xl border border-slate-300 bg-white px-4 py-3 text-sm font-bold text-slate-700 transition hover:-translate-y-0.5 hover:border-slate-400 hover:bg-slate-50 dark:border-slate-600 dark:bg-slate-900 dark:text-slate-200 dark:hover:bg-slate-800">Agora não</button>
                  </form>
                </div>
              </div>
            <% }) %>
          </div>
        <% } else { %>
          <div class="mt-4 rounded-[24px] border border-dashed border-slate-300 bg-slate-50 px-6 py-8 text-center text-sm font-semibold leading-6 text-slate-500 dark:border-slate-700 dark:bg-slate-800/60 dark:text-slate-300">Nesse mês a lupa não encontrou sugestões novas dignas de confirmação. Quando surgir padrão com cara de repeteco, ele aparece aqui para você decidir sem chute.</div>
        <% } %>
      </div>
    </section>
      </div>
    </div>
  </section>
</div>

<%- include("partials/footer") %>
