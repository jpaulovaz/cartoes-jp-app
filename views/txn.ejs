<%- include("partials/header", { title: "Distribuir item" }) %>
<div class="mb-4 flex items-center justify-between">
  <div>
    <h1 class="text-xl font-semibold">Distribuir item</h1>
    <div class="text-sm text-slate-600"><span class="badge"><%= txn.card_name %></span> • <%= (txn.txn_date || "-") %> • <%= txn.card_number || "-" %></div>
  </div>
  <a class="px-4 py-2 rounded-xl border border-slate-300 bg-white hover:bg-slate-50" href="/month/<%= txn.year %>/<%= txn.month %>">Voltar</a>
</div>
<% if (isClosed) { %>
  <div class="mb-4 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm font-semibold text-amber-900">
    🔒 Este mês está fechado. A distribuição deste item está em somente leitura.
  </div>
<% } %>
<div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-5">
  <div class="text-sm text-slate-700 mb-2"><b>Descrição:</b> <%= txn.description %></div>
  <div class="text-sm text-slate-700 mb-4"><b>Valor:</b> <%= formatBRLFromCents(txn.amount_cents) %></div>
  <form method="post" action="/txn/<%= txn.id %>" data-installment-scope-form="alloc" data-has-future-installments="<%= hasFutureInstallments ? '1' : '0' %>" data-installment-label="<%= txn.description %>">
    <h2 class="font-semibold mb-2">Pessoas (divide em partes iguais)</h2>
    <div class="grid md:grid-cols-3 gap-2 mb-4">
      <% people.forEach(p => { %>
        <label class="border border-slate-200 rounded-xl p-3 flex items-center gap-2">
          <input type="checkbox" name="person_ids" value="<%= p.id %>" <%= selected.includes(p.id) ? "checked" : "" %> <%= isClosed ? "disabled" : "" %> />
          <span><%= p.name %></span>
        </label>
      <% }) %>
    </div>
    <% if (isClosed) { %>
      <span class="inline-flex px-4 py-2 rounded-xl border border-amber-200 bg-amber-50 text-sm font-semibold text-amber-900">Mês fechado</span>
    <% } else { %>
      <% if (hasFutureInstallments) { %>
        <div class="mb-4 rounded-2xl border border-sky-200 bg-sky-50 px-4 py-3 text-sm font-semibold text-sky-900">
          Este lançamento faz parte de um parcelamento. Ao salvar ou excluir, você poderá escolher entre aplicar só nesta parcela ou nesta e nas próximas.
        </div>
      <% } %>
      <div class="flex flex-wrap items-center gap-3">
        <button class="px-4 py-2 rounded-xl bg-slate-900 text-white">Salvar</button>
        <button type="submit" form="delete-txn-form" class="px-4 py-2 rounded-xl border border-red-200 bg-red-50 text-red-700 font-semibold hover:bg-red-100">Excluir</button>
      </div>
    <% } %>
  </form>

  <% if (!isClosed) { %>
    <form id="delete-txn-form" method="post" action="/txn/<%= txn.id %>/delete" data-confirm-prompt="Excluir este lançamento? Esta ação remove o item e a distribuição associada." data-confirm-phrase="Eu confirmo" data-installment-scope-form="delete" data-has-future-installments="<%= hasFutureInstallments ? '1' : '0' %>" data-installment-label="<%= txn.description %>"></form>
  <% } %>
</div>
<%- include("partials/footer") %>
